# Business Brain — Edge Function secrets
#
# Apply with: supabase secrets set --env-file docs/business-brain/.env.example
# Do NOT commit a real .env file with these values.

# Vertex AI (clinical + patient agents)
VERTEX_PROJECT_ID=
VERTEX_REGION=asia-south1
VERTEX_ACCESS_TOKEN=

# Consumer Gemini (fallback embedder)
GEMINI_API_KEY=

# Groq (pharmacy agent — no PHI)
GROQ_API_KEY=

# Hospital contact in pre-visit messages
HOSPITAL_CONTACT=+91-22-XXXXXXXX
