node_modules

dist/
.env
.env.local
.vercel
# Vercel env pulls — contain live secrets (service_role key, tokens), never commit
.vercel.tmp.env
*.tmp.env

# macOS system files
.DS_Store
**/.DS_Store

# Tally export files — contain financial data, never commit
tally-exports/*.xml
tally-exports/*.XML
.env*.local

# Dev server and test artifacts
dev-server.log
e2e-test-results/
e2e/*.spec.ts
.firecrawl/
.env*

# Per-machine Claude Code settings (shared rules live in .claude/settings.json)
.claude/settings.local.json
