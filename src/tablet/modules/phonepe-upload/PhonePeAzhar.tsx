import { PhonePeUploadFlow } from "./PhonePeUploadFlow";

/** Azhar's payment phone — upload only; the exceptions are Tilak's tile. */
export default function PhonePeAzhar() {
  return <PhonePeUploadFlow holder="Azhar" />;
}
