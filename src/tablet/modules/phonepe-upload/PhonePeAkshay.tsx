import { PhonePeUploadFlow } from "./PhonePeUploadFlow";

/** Akshay's payment phone — upload only; the exceptions are Tilak's tile. */
export default function PhonePeAkshay() {
  return <PhonePeUploadFlow holder="Akshay" />;
}
