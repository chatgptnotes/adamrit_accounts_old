import { PhonePeUploadFlow } from "./PhonePeUploadFlow";

/** Nisha's payment phone — upload only; the exceptions are Tilak's tile. */
export default function PhonePeNisha() {
  return <PhonePeUploadFlow holder="Nisha" />;
}
